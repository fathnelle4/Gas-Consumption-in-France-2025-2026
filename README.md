# Gas-Consumption-in-France-2025-2026
It is a Spark Project

## Setup

```bash
python -m venv venv
source venv/bin/activate  # ou venv\Scripts\activate
pip install -r requirements.txt
python -m ipykernel install --user --name gas_prices_env
jupyter notebook